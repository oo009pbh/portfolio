import styled from '@emotion/styled';
export const BottomTaskBar = styled.footer`
	position: fixed;
	left: 0;
	bottom: 0;

	width: 100%;
	height: 48px;
	flex-shrink: 0;
	background: linear-gradient(90deg, #e4eefa 0%, #d5dcf6 52.4%, #e3edf6 100%);
`;
